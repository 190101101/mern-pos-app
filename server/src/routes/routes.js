const bill = require('./bill.js');
const category = require('./category.js');
const product = require('./product.js');
const user = require('./user.js');
const auth = require('./auth.js');

module.exports = {
    bill, category, product, user, auth,
};
